import PlaceholderPage from "./PlaceholderPage";

export default function Vendors() {
  return (
    <PlaceholderPage 
      title="Vendor Management" 
      description="Manage vendors, assign to projects, and configure redirect URLs"
    />
  );
}
